@model IEnumerable<BlackMesa.Blog.Model.Comment>

<section id="comment-index">
    <h1>Comments (@Model.Count())</h1>

    @foreach(var comment in Model)
    {
        @Html.Partial("Comment/_Details", comment)
    }

</section>